services:
  backend:
    build: .
    environment:
      ADG_CONTROL_PLANE_DATABASE_URL: sqlite:///./data/adg-control-plane.db
      ADG_SECRET_KEY: change-me-to-a-long-random-secret
    ports:
      - "8000:8000"
    command: >
      sh -c "python examples/seed_demo.py --database-url sqlite:///./data/adg-control-plane.db &&
      uvicorn adg.app.main:create_app --factory --host 0.0.0.0 --port 8000"
    volumes:
      - adg-data:/app/data

  web:
    image: node:20-alpine
    working_dir: /app/web
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    depends_on:
      - backend

volumes:
  adg-data:
